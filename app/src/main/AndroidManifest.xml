<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kit.showkitapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShowkitApp">
        <activity android:name=".WelcomeBackActivity"></activity>
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".VerifyOtpActivity" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PasswrdReturningActivity" />
        <activity android:name=".GenderBirthActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ContinueMobActivity" />
        <activity android:name=".EnterPasscodeActivity" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".OtpScreen" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ProfileAddActivity" />
    </application>

</manifest>